name: Test

on:
  workflow_dispatch
  
jobs:
  workflow-afterdeploy-test:
    name: Tests
    if: ${{ false }}
    uses: ./.github/workflows/matrix.yml
    
  workflow-fit:
    name: Fit
    needs: [ workflow-afterdeploy-test ]
    uses: ./.github/workflows/matrix.yml
